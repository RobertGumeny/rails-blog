<h1>Blog Posts</h1>
<% @posts.each do |post| %>
  <div class="card bg-light col-3 m-2">
    <div class="card-body">
      <h3><%= post.title %></h3>
      <p><%= post.body %></p>
      <%= link_to "Read More", post_path(post), :class => 'btn btn-sm btn-dark' %>
    </div>
  </div>
<% end %>